services:
  - type: web
    name: branded-food-filter
    runtime: python
    plan: free
    buildCommand: python3 download_metrics_db.py
    startCommand: python3 food_filter_app.py --host 0.0.0.0 --port $PORT
    envVars:
      - key: METRICS_DB_URL
        sync: false
